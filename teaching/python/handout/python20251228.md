# Python Handout(2025.12.28)
## Tabel of Contents
- [Python Handout(2025.12.28)](#python-handout20251228)
  - [Tabel of Contents](#tabel-of-contents)
  - [Textbook: Think Python third edition](#textbook-think-python-third-edition)
  - [How to preview the markdown code of handout in VS Code?](#how-to-preview-the-markdown-code-of-handout-in-vs-code)
  - [Why Python is the language of AI: insights from Guido van Rossum (creator of Python)](#why-python-is-the-language-of-ai-insights-from-guido-van-rossum-creator-of-python)
- [Conditionals statement(chapter 5)](#conditionals-statementchapter-5)
  - [if statement](#if-statement)
  - [Chained conditionals](#chained-conditionals)
  - [Nested conditionals](#nested-conditionals)
- [Iteration and Loop statement(chapter 7)](#iteration-and-loop-statementchapter-7)
  - [**For** statement](#for-statement)
  - [The **while** statement](#the-while-statement)
  - [**break** and **continue** Statements](#break-and-continue-statements)
  - [**pass** and **match** Statement](#pass-and-match-statement)
- [Functin statement(chapter 3-4)](#functin-statementchapter-3-4)
  - [Defining new functions](#defining-new-functions)
  - [Functions Call](#functions-call)
  - [Python Main Function](#python-main-function)
    - [Conclusion](#conclusion)
  - [Return value(chapter 6)](#return-valuechapter-6)
  - [A simple game programming written in Python](#a-simple-game-programming-written-in-python)
  - [Lambda Expressions](#lambda-expressions)
  - [Function Annotations](#function-annotations)

## Textbook: [Think Python third edition](https://allendowney.github.io/ThinkPython/)

## How to preview the markdown code of handout in VS Code?
> Launch VS Code and press **Ctrl + Shift + v** or **Ctrl + Shift + p** then select "Markdown: Open Preview"

## [Why Python is the language of AI: insights from Guido van Rossum (creator of Python)](https://www.youtube.com/watch?v=QXE5rEVlu20)

# Conditionals statement(chapter 5)
##  if statement
- **if** statement
```shell
if condition :
    statement
```
```python
if x > 0 :
    print("x is positive")
```
- **else** clause
```python
x = 25
if x % 2 == 0:
    print('x is even')
else:
    print('x is odd')
```
## Chained conditionals
```python
if x < y:
    print('x is less than y')
elif x > y:
    print('x is greater than y')
else:
    print('x and y are equal')
```

## Nested conditionals
```python
if x == y:
    print('x and y are equal')
else:
    if x < y:
        print('x is less than y')
    else:
        print('x is greater than y')
```
# Iteration and Loop statement(chapter 7)
## **For** statement
```python
for i in range(5):
    print(i, end=' ')
# >>> 0 1 2 3 4
```
```python
for i in range(5,10):  
    print(i, end=' ')
# >>> 5 6 7 8 9
```
## The **while** statement
```python
for i in range(1, 6):
    if (i <= 5):
        print("i value is: ", i)
        i = i + 1
```
```python
i = 1
while(i <= 5):
    print("i value is: ", i)
    i = i + 1
```
## **break** and **continue** Statements
```python
for n in range(2, 10):  
    for x in range(2, n):    
        if n % x == 0:   
            print(f'{n} equals {x} * {n//x}')   
            break
```

```python
for num in range(2, 10):    
    if num % 2 == 0:
        print(f"Found an even number {num}")    
        continue    
    print(f"Found an odd number {num}")
```
## **pass** and **match** Statement
```python
while True:  
    pass  # Busy-wait for keyboard interrupt (Ctrl+C)
```
```python
def http_error(status):
    match status:
        case 400:
            return "Bad request"
        case 404:
            return "Not found"
        case 418:
            return "I'm a teapot"
        case _:
            return "Something's wrong with the internet"
```
- A menu was written by Python
```python
while True:
    print("\n--- Welcome to the Main Menu ---")
    print("1. Add Student.")
    print("2. Delete Student.")
    print("3. Find Student.")
    print("4. Exit Program")
    print("--------------------------------")

    selection = input("Please Select(1-4): ")
    if selection == '1':
        print("add a student")
    elif selection == '2':
        print("delete a student")
    elif selection == '3':
        print("find the student")
    elif selection == '4':
        break
    else:
        print("Unknown Option Selected!")
```
# Functin statement(chapter 3-4)
## Defining new functions
```python
def function_name(parameter1, parament2, ...): # define the function
    """A docstring is a string at the beginning of a function that explains the interface.

    By convention, docstrings are triple-quoted strings, also known as multiline strings because the triple quotes allow the string to span more than one line.
    """
	statements 

function_name(argument1, argument2, ...) # function call
```
> an example of add() function
```python
def add(a, b):
    return a + b

# add a empty line
sum = add(2, 3) # call add function
print(sum)
```
## Functions Call
```python
def happybirthday(name):
    print("Happy Birthday to you!")
    print("Happy Birthday to you!")
    print("Happy Birthday, dear " + name)
    print("Happy Birthday to you!")

happybirthday("Elvis")
```
> A new version of the lyrics of “Happy Birthday Song”. Compare the first version of lyrics with the new one which was called **refactoring** in programming fundamentals.
```python
# filename: birthday.py
def repeat(word, n):
    print(word * n)

def happybirthday(name):
    argWords = "Happy Birthday to you!\n"
    repeat(argWords, 2)
    repeat("Happy Birthday, dear " + name, 1)
    repeat(argWords, 1)

if __name__=="__main__":
    happybirthday("Elvis")
```
## Python Main Function
- An example of without Main Function in Python
```python
# filename: witout_main.py
def add(a, b):
    return a + b

print("This is title of add function.")
sum = add(1, 2)
print(sum)
```
- A function call the without_main function
```python
## filename: call_withoutmain.py
from without_main import add
def prtTitle():
    print("__name__ is " + __name__ + ".")

if __name__ == '__main__':
    prtTitle()
    print(add(1, 2))
```

- An example of Main Function in Python
```python
# filename: with_main.py
def add(a, b):
    return a + b

if __name__ == '__main__':
	print("This is title of add function.")
	sum = add(1, 2)
	print(sum)
```
- A function call the without_main function
```python
## filename: call_withmain.py
from with_main import add
def prtTitle():
    print("__name__ is " + __name__ + ".")

if __name__ == '__main__':
    prtTitle()
    print(add(1, 2))
```
> The result of function call without_main and with_main as following figure
![](./images/functionCall.png)

### Conclusion
> Call a function from without main function will **run the codes of without main function first**, then run the codes of call fuction.

## Return value(chapter 6)
- If Python did not provide abs, we could write it like this.
```python
def absolute_value(x):
    if x < 0:
        return -x
    else:
        return x
if __name__ == '__main__':
    return_value = absolute_value(-5)
    print(return_value)
```
- Return the boolean values True and False
```python
def gpass(grade):
    if grade >= 60:
        return True
    else:
        return False
if __name__=='__main__':
    result = gpass(64)
    if result :
        print("Your grade was 'PASS'.")
    else:
        print("Your grade was 'NO PASS'.")

```
> **Return True or False** to print the "PASS" or "NO PASS" on screen.
![](./images/gpass.png)

## A simple game programming written in Python
> Greater Than or Less Than" game codes in Python
```python
import random

def main_menu():
    print("\n" + "="*30)
    print(" GREATER OR LESS THAN GAME ")
    print("="*30)
    print("Rules:")
    print("1. The computer picks a random number (0-11).")
    print("2. You choose 'Greater' or 'Less'.")
    print("3. Enter your own number (1-10).")
    print("4. You win if your logic matches the result!")
    print("="*30)

def play_game():
    main_menu()
    
    # 1. Generate computer's random number
    computer_num = random.randint(0, 11)
    
    # 2. Get user's choice (Greater or Less)
    choice = input("Will your number be (G)reater or (L)ess than the computer's? ").strip().upper()
    while choice not in ['G', 'L']:
        choice = input("Invalid! Please enter 'G' for Greater or 'L' for Less: ").strip().upper()

    # 3. Get user's number input
    try:
        user_num = int(input("Enter your number (1-10): "))
        if not (1 <= user_num <= 10):
            print("Number must be between 1 and 10!")
            return
    except ValueError:
        print("Please enter a valid whole number.")
        return

    print(f"\nComputer's number was: {computer_num}")
    print(f"Your number: {user_num}")

    # 4. Compare and determine winner
    if choice == 'G':
        if user_num > computer_num:
            print("🎉 YOU WIN! Your number is greater.")
        else:
            print("❌ YOU LOSE! Your number was not greater.")
    elif choice == 'L':
        if user_num < computer_num:
            print("🎉 YOU WIN! Your number is less.")
        else:
            print("❌ YOU LOSE! Your number was not less.")

# Start the game
if __name__ == "__main__":
    while True:
        play_game()
        again = input("\nPlay again? (y/n): ").lower()
        if again != 'y':
            print("Thanks for playing!")
            break
```
> The game programming show on the screen.
![](./images/game.png)

>> Exercise of each chapters was very important to improve your programming skill in Python. We hope who is a beginner of Python, should write these codes of each chapter's exercise by himself/herself.
## Lambda Expressions
- Lambda Syntax
```python
lambda parameter: expression
```
```python
add = lambda a,b: a + b
add(3,4) #output: 7
(lambda a, b: a * b)(3,4) #output: 12
```
```python
def make_incrementor(n):
    return lambda x: x + n
if __name__ == "__main__":
    f = make_incrementor(42)
    print(f(0)) #output: 42
    print(f(1)) #output: 43
```
```python
addition = lambda *para: sum(para)
addition(20,10,11) #output: 41
```
## Function Annotations
- Function annotations are completely optional metadata information about the types used by user-defined functions.
- **Parameter annotations are defined by a colon after the parameter name**, followed by an expression evaluating to the value of the annotation.
- **Return annotations are defined by a literal ->**, followed by an expression, between the parameter list and the colon denoting the end of the def statement.
```python
def f(ham: str, eggs: str = 'eggs') -> str:
    print("Annotations:", f.__annotations__)
    print("Arguments:", ham, eggs)
    return ham + ' and ' + eggs

f('spam')
```
> 